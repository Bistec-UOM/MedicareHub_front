import { Dialog } from '@mui/material';
import React from 'react';

const UserAddToList = (RestoreOpen) => {
    return (
        <div>
            <Dialog open={RestoreOpen} >
                hihi
            </Dialog>
        </div>
    );
}

export default UserAddToList;
